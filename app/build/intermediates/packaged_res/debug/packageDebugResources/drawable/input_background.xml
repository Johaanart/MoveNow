<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color de fondo -->
    <solid android:color="@android:color/white" />

    <!-- Borde del recuadro -->
    <stroke
        android:width="1.5dp"
        android:color="@color/azul_texto" />

    <!-- Bordes redondeados -->
    <corners android:radius="12dp" />

    <!-- Espaciado interno (opcional, ayuda a que se vea más limpio) -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />

</shape>
